.env
google-credentials.json
# Covers backups too (google-token.json.bak etc) -- these hold a live
# refresh token and must never be committed.
google-token.json*
google-credentials.json*
processed_pos.json
__pycache__/
*.pyc
venv/

po_test.csv
po_test_2.csv

# WSL/Windows NTFS Alternate Data Stream artifacts
*:Zone.Identifier
# Local listener output; the deployed listener logs to stdout instead.
listener.log
